<% if (messages) { %>
    <% messages.forEach((message) => { %>
        <div class="alert alert-<%= message.type %>" role="alert">
            <%= message.text %>
        </div>
    <% }) %>
    <% removeMessages() %>
<% } %>
