<entries>
    <% entries.forEach((entry) => { %>
        <entry>
            <title><%= entry.title %></title>
            <body><%= entry.body %></body>
            <author><%= entry.name %></author>
        </entry>
    <% }) %>
</entries>
